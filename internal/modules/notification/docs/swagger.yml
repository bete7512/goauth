# Notification Module - Pure Delivery Layer
# This module has no HTTP routes. It listens to events from the core module
# and delivers emails/SMS via configured senders.
# All verification and password reset endpoints are in the core module.

paths: {}

tags:
  - name: Notification Module
    description: Notification Module - Pure delivery layer for email/SMS notifications. No HTTP endpoints.
